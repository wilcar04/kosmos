import 'package:flutter/material.dart';

class ContinentCard extends StatelessWidget {
  const ContinentCard({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
